{
  "name": "cjs-local",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "docker run --rm -i -v $(cd ../../ && pwd):/app:ro -w /app/itest/cjs-local --env-file env.env node:lts node index.js"
  }
}
